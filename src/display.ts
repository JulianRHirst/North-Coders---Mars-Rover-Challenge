import{Box, Point} from "geometry";
import assert from 'node:assert/strict';

class Display {
    area: Box;
    buffer : string[][];

    constructor(height: number, width: number) {
        
        this.height = height; 
        this.width = width; 
        this.origin = {...origin};
        assert(height > 0 && width > 0 and  )
    }
    
}